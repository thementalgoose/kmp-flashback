package tmg.flashback.device.usecases

actual class ShareWebpageUseCaseImpl actual constructor() :
    ShareWebpageUseCase {
    actual override operator fun invoke(url: String, title: String) {
    }
}