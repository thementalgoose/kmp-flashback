package tmg.flashback.device.usecases

import tmg.flashback.device.models.Permission

actual class RequestPermissionUseCaseImpl actual constructor(): RequestPermissionUseCase {
    actual override fun invoke(permission: Permission) {
        // TODO
    }
}